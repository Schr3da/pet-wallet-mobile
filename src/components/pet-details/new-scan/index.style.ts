import type {ITheme} from "../../../theme";

export const applyStyles = (_: ITheme) => ({
  scrollContainer: {
    flex: 1,
  },
  placeholderIcon: {
    maxWidth: 112,
    maxHeight: 140,
  },
  picker: {
    marginTop: 20,
    marginBottom: 20,
  },
  attachmentsWrapper: {
    width: 280,
  },
  attachment: {
    marginBottom: 10,
  },
});
